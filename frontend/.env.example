# Backend API base URL (local dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# NeonDB connection string (read-only Server Component queries)
# Format: postgresql://[user]:[password]@[host]/[dbname]?sslmode=require
DATABASE_URL=
